# 개발 환경(dev) 로컬 설정
#
# 주의: 이 파일은 로컬 개발 환경에서만 사용됩니다.
# 스테이징(stg)과 프로덕션(prod) 환경은 Infisical에서 관리됩니다.
#
# 사용법:
# 1. 이 파일을 .env로 복사: cp .env.example .env
# 2. 아래 값들을 실제 값으로 변경

# Database (MySQL)
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=podpod_dev
MYSQL_USER=root
MYSQL_PASSWORD=your-mysql-password-here

# Server
PORT=8000

# Security
# 생성 방법: python3 -c 'import secrets; print(secrets.token_urlsafe(32))'
# SECRET_KEY=

# KAKAO OAuth
# - Kakao 개발자 콘솔에 등록된 Redirect URI와 일치해야 함
# - 다른 포트를 사용할 경우 Kakao 콘솔에서 해당 포트를 추가해야 함
KAKAO_CLIENT_ID=your-kakao-client-id
KAKAO_CLIENT_SECRET=your-kakao-client-secret
KAKAO_REDIRECT_URI=http://localhost:8000/api/v1/auth/kakao/callback

# Google OAuth
# - Google Cloud Console에 등록된 Redirect URI와 일치해야 함
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_REDIRECT_URI=http://localhost:8000/api/v1/auth/google/callback

# Apple OAuth
# - Apple Developer Console에 설정된 값과 일치해야 함
APPLE_CLIENT_ID=your-apple-client-id
APPLE_TEAM_ID=your-apple-team-id
APPLE_KEY_ID=your-apple-key-id
APPLE_PRIVATE_KEY=your-apple-private-key
APPLE_SCHEME=your-apple-scheme

# Sendbird
# - Sendbird Dashboard에서 발급받은 값 사용
SENDBIRD_APP_ID=your-sendbird-app-id
SENDBIRD_API_TOKEN=your-sendbird-api-token

# Google Sheets API
# - Google Cloud Console에서 Sheets API 활성화 필요
# - Service Account 생성 후 JSON 키 발급
GOOGLE_SHEETS_ID=your-google-sheets-id
GOOGLE_SHEETS_CREDENTIALS=your-google-sheets-credentials-json

# Firebase (푸시 알림)
# - Firebase Console에서 Service Account 키 발급
# - JSON 형식의 Service Account Key를 한 줄로 변환하여 입력
FIREBASE_SERVICE_ACCOUNT_KEY=your-firebase-service-account-key-json
